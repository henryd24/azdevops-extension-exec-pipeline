{
  "name": "exec-pipeline-task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "testdev": "jest --watch",
    "coverage": "jest --coverage",
    "build": "tsc -p tsconfig.build.json",
    "test-local": "export $(grep -v '^#' .env | xargs -0); bun src/exec_pipeline.ts",
    "configure": "cp -r task.json icon.png ../../../tasks/exec-pipeline",
    "package": "ncc build src/exec_pipeline.ts -o ../../../tasks/exec-pipeline && npm run configure"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "^5.2.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/mocha": "^10.0.1",
    "@types/node": "^24.10.0",
    "@types/q": "^1.5.5",
    "@vercel/ncc": "^0.38.4",
    "jest-fetch-mock": "^3.0.3",
    "ts-jest": "^29.1.2",
    "typescript": "^5.7.2"
  }
}
